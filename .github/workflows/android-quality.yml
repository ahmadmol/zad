name: Android Quality Gates

on:
  push:
    branches: [ main, master, 'chore/**', 'refactor/**', 'release/**' ]
  pull_request:

jobs:
  build-test-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Assemble debug
        run: ./gradlew :app:assembleDebug --stacktrace

      - name: Assemble release (unsigned/debug-signed fallback; no secrets required)
        run: ./gradlew :app:assembleRelease --stacktrace

      - name: Unit tests
        run: |
          ./gradlew :designsystem:testDebugUnitTest \
            :feature:testDebugUnitTest \
            :app:testDebugUnitTest \
            --stacktrace

      - name: Lint
        run: ./gradlew :app:lintDebug --no-parallel --stacktrace

      - name: Upload reports on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: android-quality-reports
          path: |
            **/build/reports/tests/
            **/build/reports/lint-results*.html
            **/build/reports/lint-results*.xml
            **/build/outputs/mapping/
          if-no-files-found: ignore

  # Instrumented tests remain manual/scheduled (device cost; no secrets).
